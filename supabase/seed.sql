-- =============================================================================
-- Seed for local `supabase db reset` runs.
-- =============================================================================
-- Initial 분류값 시드는 마이그레이션(20260526120000_initial_schema.sql) 마지막
-- 블록에 idempotent insert로 포함되어 있어 별도 seed.sql 없이도 채워진다.
-- 이 파일은 로컬 개발에서 빈 DB로 시작할 때 추가로 넣고 싶은 sample row
-- (테스트용 catalog 등)를 두기 위해 비워둔다.
-- =============================================================================
